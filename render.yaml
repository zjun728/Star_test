# Render Blueprint: 星盘查询 API
# 文档 https://render.com/docs/blueprint-spec
services:
  - type: web
    name: star-chart-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7
